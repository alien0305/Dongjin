const message = () => {
                        const name = "Dongja";
                        const age = 32;
                        return name + ' is ' + age + ' years old.';
                      };
// default를 쓰면 import할때 중괄호를 안써도 됨    
export default message;